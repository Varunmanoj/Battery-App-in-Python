# Create submenu
# subMenu = tk.Menu(ViewMenu, tearoff=0)
# subMenu.add_command(label="Small Font", command=smallsize)
# subMenu.add_command(label="DefaultFont", command=DefaultSize)
# subMenu.add_command(label="Large Font", command=LargeSize)